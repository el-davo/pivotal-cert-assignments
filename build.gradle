buildscript {
    repositories {
        maven { url "http://platform-acceleration-lab-maven.s3.amazonaws.com" }
        jcenter()
    }

    dependencies {
        classpath "io.pivotal.pal.assignments:assignments-plugin:4.3.1"
    }
}

apply plugin: "io.pivotal.pal.assignments.plugin"

assignments {
    apiUrl = "https://dell-waveland.education.pivotal.io/"
    emails = [
       "david.ahern@dell.com",
    ]
    cohortIdentifier = "pal-java-cnd-online-psapps"
}

defaultTasks "tasks"
